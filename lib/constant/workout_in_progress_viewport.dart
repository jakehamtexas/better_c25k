import 'package:flutter/material.dart';

class WorkoutInProgressButton {
  static const buttonShape = CircleBorder();
  static const buttonSize = 100.0;
  static const splashColor = Colors.transparent;
  static const highlighColor = Colors.transparent;
}
