import 'package:flutter/material.dart';

class WorkoutMapLoadingIndicator extends StatelessWidget {
  const WorkoutMapLoadingIndicator({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
