import 'package:flutter/material.dart';

class WorkoutMapNetworkError extends StatelessWidget {
  const WorkoutMapNetworkError({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
