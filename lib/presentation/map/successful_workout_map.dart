import 'package:flutter/material.dart';

class SuccessfulWorkoutMap extends StatelessWidget {
  const SuccessfulWorkoutMap({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
