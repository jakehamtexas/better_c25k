import 'package:flutter/material.dart';

class SaveSuccessfulSnackBar extends SnackBar {
  const SaveSuccessfulSnackBar()
      : super(
          content: const Text("Save successful!"),
        );
}
