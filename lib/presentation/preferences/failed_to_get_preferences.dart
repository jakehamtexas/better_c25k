import 'package:flutter/material.dart';

class FailedToGetPreferences extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Text("Failed to get preferences!");
  }
}
