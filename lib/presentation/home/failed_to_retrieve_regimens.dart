import 'package:flutter/material.dart';

class FailedToRetrieveRegimens extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text("We failed to get the regimens! 😭");
  }
}
